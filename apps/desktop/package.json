{
  "name": "@tessera/desktop",
  "version": "0.0.0",
  "private": true,
  "description": "The Tessera desktop app (Tauri 2). Its TypeScript side exports the desktop services that the web desktop feature registers.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsdown",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0"
  },
  "dependencies": {
    "@tauri-apps/api": "2.11.1",
    "@tessera/core": "workspace:*"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.5"
  }
}
