/** Device setting: the workspace the first-run "join" button created (forgotten once joined). */
export const JOIN_WORKSPACE_KEY = 'sync.joinWorkspace';

/** Device setting: an empty workspace created only to join a server one, removed once joined. */
export const TEMP_WORKSPACE_KEY = 'sync.removeWorkspace';
