/**
 * @tessera/markdown — markdown and HTML to and from the canonical schema (Agent 08).
 *
 * Implements `MarkdownCodec` (priority 50), registered through
 * `apps/web/src/features/import-export`. See HANDOFF/architect.md.
 */
export const MARKDOWN_PACKAGE = '@tessera/markdown';
