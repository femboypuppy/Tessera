import { createTranslator } from '@tessera/ui';
import { en } from './en';

/** `t` for the `search` namespace: `t('palettePlaceholder')`, or `t('search:…')` anywhere. */
export const t = createTranslator('search', en);
