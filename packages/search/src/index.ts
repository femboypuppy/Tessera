/**
 * @tessera/search — search index, command palette, backlinks and graph (Agent 05).
 *
 * Provides the MiniSearch SearchIndex and the graph LinkIndex (priority 50), registered through
 * `apps/web/src/features/search`, `graph` and `backlinks`. See HANDOFF/architect.md.
 */
export const SEARCH_PACKAGE = '@tessera/search';
