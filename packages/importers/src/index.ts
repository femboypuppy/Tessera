/**
 * @tessera/importers — Notion, Obsidian and markdown importers and exporters (Agent 08).
 *
 * Implements `Importer` and `Exporter` from `@tessera/core`, registered through
 * `apps/web/src/features/import-export`. See HANDOFF/architect.md.
 */
export const IMPORTERS_PACKAGE = '@tessera/importers';
