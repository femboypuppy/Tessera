/**
 * @tessera/editor — the block editor (Agent 02).
 *
 * Implements the canonical document schema from `@tessera/core` with TipTap on Yjs, and registers
 * itself as the `page` body through `apps/web/src/features/editor`. See HANDOFF/architect.md.
 */
export const EDITOR_PACKAGE = '@tessera/editor';
