{
  "name": "@tessera/plugin-api",
  "version": "0.0.0",
  "private": true,
  "description": "The SDK for writing Tessera plugins.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsdown",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0"
  },
  "dependencies": {
    "@tessera/core": "workspace:*"
  }
}
