/**
 * @tessera/plugins — the sandboxed plugin host and plugin manager UI (Agent 06).
 *
 * Built on the `PluginManifest` schema and permission names from `@tessera/core`, registered through
 * `apps/web/src/features/plugins`. See HANDOFF/architect.md.
 */
export const PLUGINS_PACKAGE = '@tessera/plugins';
