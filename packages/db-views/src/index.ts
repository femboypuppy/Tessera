/**
 * @tessera/db-views — the query engine and database views (Agent 04).
 *
 * Renders `kind: 'database'` pages through `pageBodies.database` and inline databases through the
 * `database` embed renderer, registered in `apps/web/src/features/databases`. See HANDOFF/architect.md.
 */
export const DB_VIEWS_PACKAGE = '@tessera/db-views';
